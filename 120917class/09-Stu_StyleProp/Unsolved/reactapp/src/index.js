import React from "react";
import ReactDOM from "react-dom";
import App from "./App";


//Make sure to use this library which does not come with React. ReactDOM.render()

ReactDOM.render(<App />, document.getElementById("root"));
